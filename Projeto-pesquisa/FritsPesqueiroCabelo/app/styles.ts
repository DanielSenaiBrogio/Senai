import { StyleSheet } from "react-native";

export const GlobalStyles = StyleSheet.create({
    image: {
        width: 250,
        height: 400
    },
    container: {
        alignItems: 'center',
        justifyContent: 'center',
    },
    textoDaImagem: {
        fontSize: 20,
        fontWeight: 'bold',
    }
});

